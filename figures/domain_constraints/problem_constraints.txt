context Problem inv:

self.problemTitle->notEmpty()

self.problemDescription->notEmpty()

self.pin.matches(^p-\d+$)

self.referenceIn->notEmpty()

self.referenceIn.matches(^b-\d+$) | self.referenceIn.matches(^r-\d+$) | self.referenceIn.matches(^c-\d+$)
self.referenceIn.matches(^b-\d+$) -> implies building.findById(self.referenceIn)->exists()
self.referenceIn.matches(^r-\d+$) -> implies room.findById(self.referenceIn)->exists()
self.referenceIn.matches(^c-\d+$) -> implies component.findById(self.referenceIn)->exists()

self.nin.matches(^n-\d+$)
self.nin.matches(^n-\d+$) -> implies notification.findById(self.nin)->exists()

self.problemState->notEmpty()

self.problemReporter->notEmpty()

self.creationDate->notEmpty()



